package com.ryan.Conceitos.QueryMethods.SpecificationsSDJ.spec;

public class RestauranteSpecs {

    //clase para construir especificações sem classes separadas
    //restaurante usado como exemplo


    //public static RestauranteComNomeSemelhanteSpec comNomeSemelhante(String nome) {
    //    return (root, query, criteriaBuilder) -> 
    //        criteriaBuilder.like(root.get("nome"), "%" + nome + "%");               //filtra restaurantes com nome semelhante ao parametro nome
    //}

    //public static RestauranteComFreteGratisSpec comFreteGratis() {
    //    return (root, query, criteriaBuilder) -> 
    //        criteriaBuilder.equal(root.get("taxaFrete"), BigDecimal.ZERO);               //filtra restaurantes com taxaFrete igual a 0
    //}
}
